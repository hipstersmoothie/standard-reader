{
  "lexicon": 1,
  "id": "app.standard-reader.bookmark",
  "defs": {
    "main": {
      "type": "record",
      "description": "A reader's saved (bookmarked) article. Subject must be the at:// URI of a site.standard.document record.",
      "key": "any",
      "record": {
        "type": "object",
        "required": ["subject", "createdAt"],
        "properties": {
          "subject": {
            "type": "string",
            "format": "at-uri",
            "description": "AT URI of the site.standard.document record being bookmarked."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
