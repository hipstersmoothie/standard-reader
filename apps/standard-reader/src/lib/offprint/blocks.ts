/**
 * `app.offprint.content` block parsing — re-exported from
 * `@standard-reader/renderer-core`, which owns it (see
 * `#/lib/document/structured-content/types`).
 */

export { offprintBlocks } from "@standard-reader/renderer-core";
