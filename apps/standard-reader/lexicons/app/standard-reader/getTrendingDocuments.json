{
  "lexicon": 1,
  "id": "app.standard-reader.getTrendingDocuments",
  "defs": {
    "main": {
      "type": "query",
      "description": "Ranked list of trending articles across the network.",
      "parameters": {
        "type": "params",
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 12
          },
          "scope": {
            "type": "string",
            "knownValues": ["rail", "page"],
            "default": "rail",
            "maxLength": 32
          }
        },
        "required": []
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["items"],
          "properties": {
            "items": {
              "type": "array",
              "items": {
                "type": "ref",
                "ref": "app.standard-reader.defs#documentView"
              }
            }
          }
        }
      }
    }
  }
}
