# Local dev (Bluesky OAuth needs 127.0.0.1, not localhost):
VITE_API_ORIGIN=http://127.0.0.1:3000

# Store builds set this automatically in the `zip` script. To build manually:
# VITE_API_ORIGIN=https://standard-reader.app pnpm extension:build
