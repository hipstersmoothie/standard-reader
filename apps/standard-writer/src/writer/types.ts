export type Screen = "write" | "drafts" | "pub" | "newpub" | "publish" | "edit";

/** Write-screen layout: focused editor vs. editor + document workbench rail. */
export type Layout = "focus" | "workbench";
