{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'ingest worker runs TypeScript via tsx; no build step'"
  },
  "deploy": {
    "startCommand": "pnpm ingest:start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
